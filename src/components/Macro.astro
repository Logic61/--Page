---
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<section class="macro">
  <div class="wrap">
    <header class="s-head reveal">
      <div class="title-row">
        <span class="eyebrow">宏观</span>
        <h2 class="section-title">巫之总体</h2>
      </div>
      <div class="rule"></div>
    </header>

    <div class="macro-prose reveal" data-d="2">
      <p>
        巫，自古神秘。氏族时代便已然开始，处于社会权力之巅，不单引领部落，且掌控「神明」与「先祖」的沟通。
      </p>
      <p>
        然而岁月无情，事无长恒。随着时代变迁，显世巫祝之地位逐渐下降——虽依然处于权力圈核心，却越发危机，直至断崖式爆发。
      </p>
      <p>
        至秦之后，显世巫祝开始<b>巫道合流</b>：大部分被五斗米道收纳，少部分融于儒教，小体则流入民间，成为所谓的「巫医」。时至今日，诸多巫祝相关与遗留，便原至于此。
      </p>
      <p>
        但真正的完整巫之传承，并未因此断绝。早在重大变故之前，本脉便提前离于世间，自古隐秘，不问世事，不管轮转，传承至今。
      </p>
    </div>

    <div class="macro-cite reveal" data-d="3">
      <p>真正的完整巫之传承并未断绝，<br />而是在重大变故前，提前离于世间。</p>
      <a class="btn" href={`${base}/chuancheng`}>阅览传承 →</a>
    </div>
  </div>
</section>

<style>
  .macro {
    border-top: 1px solid var(--ink-line);
  }

  .macro-prose {
    max-width: 46em;
  }

  .macro-prose p {
    color: var(--bone-dim);
    font-size: 1.02rem;
    line-height: 2.2;
    margin-bottom: 1.4rem;
  }

  .macro-prose b {
    color: var(--bronze-bright);
    font-weight: 600;
  }

  .macro-cite {
    margin-top: clamp(2.5rem, 5vw, 4rem);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    flex-wrap: wrap;
    border-top: 1px solid var(--ink-line);
    border-bottom: 1px solid var(--ink-line);
    padding: 1.8rem 0;
  }

  .macro-cite p {
    font-family: var(--font-kai);
    font-size: clamp(1.1rem, 2.4vw, 1.6rem);
    letter-spacing: 0.16em;
    line-height: 2;
    color: var(--bronze-bright);
  }
</style>